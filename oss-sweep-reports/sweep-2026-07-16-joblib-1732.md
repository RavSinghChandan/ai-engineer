# OSS sweep — 2026-07-16 — CANDIDATE FOUND (engagement pending)

**VERDICT: CANDIDATE FOUND — joblib #1732 (maintainer-confirmed, unclaimed, reproduces on macOS).**

## joblib/joblib #1732 — Pydantic models not (un)pickled correctly with _LokyPickler
- URL: https://github.com/joblib/joblib/issues/1732
- Maintainer `lesteve` (MEMBER) confirmed: "it seems like a real bug", kept open. Unassigned, no open PR.
- User has 2 merges in joblib already → credibility to engage.

## What I verified locally (Python 3.9, pydantic 2.13, joblib main)
- Bug REPRODUCES end-to-end via `Parallel(n_jobs=2)` with a model that has `@model_validator(mode='before')`. Error: `BrokenProcessPool` → worker `AttributeError: _pre` from pydantic `ModelMetaclass.__getattr__` (`_model_construction.py:290`) during un-serialize.
- **Issue author's workaround (move model to own module) does NOT actually fix it** — I tested, still fails. Important correction to the thread.
- In ISOLATION every joblib serialization path works: cloudpickle direct OK, loky dumps→loads OK, loky dumps→pickle.loads OK. So the "_LokyPickler pickles it wrong" theory in the issue is likely WRONG — failure is specific to reconstructing the `@model_validator` method in a fresh worker → looks more pydantic-side than joblib-side.

## Comment POSTED by Chandan 2026-07-16
He posted the honest, well-hedged comment on the issue (flags workaround doesn't work, gives AttributeError: _pre root cause, notes isolated paths pass, asks lesteve: pydantic-upstream or joblib-side guard?). Now WAITING for lesteve's reply. Do NOT post again or PR until he answers (engagement-first). If ~1 week silence, one light "gentle ping" is OK.

## FURTHER investigation (2026-07-16, after posting) — narrowed the culprit
CORRECTION to earlier lean: it is NOT purely pydantic's fault.
- Pure stdlib pickle of the model across a fresh subprocess: WORKS.
- loky dumps of the model alone across a fresh subprocess: WORKS (identical 154-byte blob to stdlib).
- So the model's OWN pickling is fine either way. The failure ONLY appears in the full `Parallel` path → culprit is how the model is wrapped inside loky's `_CallItem` / `BatchedCalls` task tuple and reconstructed, NOT the model itself. Deeper than the thread realized.
- Next focused dig (once lesteve picks a direction): trace loky BatchedCalls/_CallItem reconstruction to find exact line where pydantic `__getattr__('_pre')` fires. Then build the precise fix for THAT layer + regression test.

## Next step
WAIT for lesteve. When he replies, paste to me → I do targeted root-cause on the layer he wants → hand Chandan verified fix + PR text to push himself.

Repro scripts: /tmp/repro_1732.py, /tmp/repro_deep.py, /tmp/repro_path.py, /tmp/repro_module.py (regenerate if /tmp cleared).
